# 관리자 대시보드 V2 - 구현 계획

## 📋 기획서 기반 구조 (Module 7)

### 핵심 작업 (총 55pt, 16 Issues)

## 1. 실시간 대시보드 (8pt)

### 📊 통계 카드
```
┌─────────────────┬─────────────────┬─────────────────┬─────────────────┐
│  금일 가입자     │  미션 완료율     │  페이백 대기    │  활성 사용자    │
│  +12 (↑15%)     │  68% (↑5%)      │  23건           │  1,234명        │
└─────────────────┴─────────────────┴─────────────────┴─────────────────┘
```

### 📈 실시간 차트
- 시간별 가입 추이 (24시간)
- 미션 타입별 완료율
- 지점별 활성 사용자

### ⚡ 실시간 알림 피드
- 신규 가입자
- 미션 완료
- 페이백 신청

## 2. 미션 생성 및 편집 (8pt)

### 🎯 미션 빌더
```typescript
interface MissionBuilder {
  // 기본 정보
  type: 'daily' | 'story' | 'challenge' | 'social'
  category: string
  title: string
  description: string

  // 스토리 미션
  chapterId?: string
  chapterOrder?: number
  missionOrder?: number

  // 난이도 및 보상
  difficulty: 'easy' | 'medium' | 'hard'
  rewards: {
    xp: number
    coins: number
    cash?: number
    badges?: string[]
  }

  // 조건
  requirements: Requirement[]
  proofRequirements: ProofRequirement[]
  unlockConditions?: UnlockCondition

  // 기간
  startDate?: Date
  endDate?: Date
  durationHours?: number

  // 타겟
  isGlobal: boolean
  storeId?: number
}
```

### 🎨 드래그 앤 드롭 빌더
- 미션 요구사항 추가/제거
- 증빙 자료 설정
- 보상 설정

### 📋 미션 템플릿
- 일일 미션 템플릿 (퀴즈, 체크인, 학습, 소셜)
- 스토리 미션 템플릿 (챕터별)
- 도전 미션 템플릿

### 👁️ 미리보기
- 모바일 뷰 미리보기
- 사용자 화면 미리보기

## 3. 사용자 관리 (5pt)

### 🔍 검색 및 필터
- 이름, 전화번호, 이메일로 검색
- 지점별 필터
- 레벨별 필터
- 가입일 필터
- 상태별 필터 (활성, 정지, 탈퇴)

### 👤 사용자 상세 정보
```typescript
interface UserDetail {
  // 기본 정보
  id: string
  name: string
  nickname: string
  phone: string
  email: string
  profilePictureUrl: string

  // 게이미피케이션
  level: number
  xp: number
  coins: number
  cashBalance: number

  // 출석
  consecutiveDays: number
  totalAttendanceDays: number

  // 통계
  totalMissionsCompleted: number
  badgesCount: number

  // 미션 참여 내역
  missions: MissionParticipation[]

  // 페이백 내역
  paybacks: Payback[]
}
```

### 🔧 계정 관리
- 정보 수정
- 계정 정지/복구
- 비밀번호 리셋
- 포인트 지급/차감

## 4. 미션 제출물 검토 (8pt)

### 🖼️ 이미지 뷰어
- 확대/축소
- 회전
- 다운로드
- 여러 이미지 비교

### ✅ 승인/거부
```typescript
interface ReviewAction {
  participationId: string
  action: 'approve' | 'reject'
  reviewedBy: string
  reviewedAt: Date
  rejectionReason?: string
  adminNotes?: string
}
```

### 📝 거부 사유 템플릿
- "증빙 자료가 불명확합니다"
- "미션 요구사항을 충족하지 않습니다"
- "중복 제출입니다"
- "사진 품질이 좋지 않습니다"
- 사용자 정의

### 📊 검토 대시보드
- 대기 중인 제출물 수
- 평균 검토 시간
- 승인/거부 비율
- 검토자별 처리량

## 5. 페이백 일괄 처리 (5pt)

### ☑️ 체크박스 선택
- 전체 선택/해제
- 조건별 선택 (금액, 지점, 날짜)

### ✅ 일괄 승인
```typescript
interface BulkPaybackApproval {
  paybackIds: string[]
  approvedBy: string
  approvedAt: Date
  totalAmount: number
}
```

### 📥 엑셀 다운로드
- 페이백 리스트 다운로드
- 필터링된 데이터 다운로드
- 지급 완료 증빙 자료

### 💰 페이백 통계
- 일별/주별/월별 지급액
- 미션별 페이백 비용
- 지점별 페이백 비용

## 6. 통계 및 분석 (8pt)

### 📊 차트 라이브러리
- Recharts 사용
- 반응형 차트

### 📈 가입 추이
- 일별/주별/월별 가입자 수
- 지점별 비교
- 추천 경로별 분석

### 🎯 미션 완료율
- 미션 타입별 완료율
- 난이도별 완료율
- 시간대별 참여율

### 🏢 지점별 비교
- 활성 사용자 수
- 미션 완료율
- 페이백 지급액

### ⏰ 기간별 필터
- 오늘
- 이번 주
- 이번 달
- 지난 30일
- 사용자 정의

## 7. 알림 관리 (3pt)

### 📢 푸시 알림 발송
```typescript
interface PushNotification {
  title: string
  message: string
  targetType: 'all' | 'store' | 'level' | 'custom'
  targetIds?: string[]
  scheduledAt?: Date
  imageUrl?: string
  actionUrl?: string
}
```

### ⏱️ 예약 발송
- 발송 시간 설정
- 반복 설정 (매일, 매주, 매월)

### 🎯 타겟 설정
- 전체 사용자
- 특정 지점
- 특정 레벨 범위
- 특정 사용자 그룹

## 8. 이벤트 관리 (5pt)

### 🎉 이벤트 생성
```typescript
interface Event {
  id: string
  title: string
  description: string
  type: 'mission' | 'reward' | 'ranking' | 'special'

  // 기간
  startDate: Date
  endDate: Date

  // 배너
  bannerUrl: string
  thumbnailUrl: string

  // 보상
  rewards: EventReward[]

  // 조건
  participationConditions: string[]

  // 상태
  isActive: boolean
  isVisible: boolean
}
```

### 🖼️ 배너 관리
- 이미지 업로드
- 순서 조정
- 노출 기간 설정

### ⏰ 기간 설정
- 시작/종료 시간
- 자동 시작/종료

## 9. 권한 관리 (3pt)

### 👑 역할 정의
```typescript
type AdminRole = 'super_admin' | 'admin' | 'staff'

interface Permission {
  resource: string
  actions: ('create' | 'read' | 'update' | 'delete')[]
}
```

### 🔐 역할별 권한 설정
- **Super Admin**: 모든 권한
- **Admin**: 지점 관리, 미션 관리, 사용자 관리
- **Staff**: 미션 검토, 페이백 처리

### 🏢 지점별 접근 제어
- 지점장은 자기 지점만 관리
- 슈퍼 관리자는 모든 지점 관리

## 10. 신고 관리 (2pt)

### 📝 신고 목록
```typescript
interface Report {
  id: string
  reporterUserId: string
  reportedUserId: string
  reportType: 'spam' | 'abuse' | 'inappropriate' | 'fraud' | 'other'
  description: string
  evidence: string[] // 이미지 URL
  status: 'pending' | 'reviewed' | 'resolved' | 'dismissed'
  createdAt: Date
}
```

### 🔍 처리 상태
- 대기
- 검토 중
- 해결 완료
- 기각

## 페이지 구조

```
/admin
├── /dashboard              # 1. 실시간 대시보드
│   ├── 통계 카드
│   ├── 실시간 차트
│   └── 알림 피드
│
├── /missions               # 2. 미션 관리
│   ├── /create            # 미션 생성
│   ├── /edit/[id]         # 미션 편집
│   ├── /templates         # 미션 템플릿
│   └── /submissions       # 4. 제출물 검토
│
├── /users                  # 3. 사용자 관리
│   ├── /list              # 사용자 목록
│   └── /[id]              # 사용자 상세
│
├── /paybacks               # 5. 페이백 처리
│   ├── /pending           # 대기 중
│   ├── /approved          # 승인 완료
│   └── /bulk              # 일괄 처리
│
├── /analytics              # 6. 통계 및 분석
│   ├── /overview          # 전체 통계
│   ├── /missions          # 미션 분석
│   └── /stores            # 지점 비교
│
├── /notifications          # 7. 알림 관리
│   ├── /send              # 알림 발송
│   └── /scheduled         # 예약 알림
│
├── /events                 # 8. 이벤트 관리
│   ├── /list              # 이벤트 목록
│   └── /create            # 이벤트 생성
│
├── /settings               # 9. 권한 관리
│   ├── /roles             # 역할 관리
│   └── /permissions       # 권한 설정
│
└── /reports                # 10. 신고 관리
    ├── /pending           # 대기 중
    └── /resolved          # 처리 완료
```

## 구현 순서

### Phase 1: 기반 구축 (1-2일)
1. ✅ 관리자 레이아웃 V2 재구성
2. ✅ 네비게이션 메뉴 구조
3. ✅ 디자인 시스템 적용

### Phase 2: 핵심 기능 (3-5일)
4. 실시간 대시보드
5. 미션 제출물 검토
6. 페이백 처리

### Phase 3: 고급 기능 (3-4일)
7. 미션 빌더
8. 사용자 관리 상세
9. 통계 및 분석

### Phase 4: 부가 기능 (2-3일)
10. 알림 관리
11. 이벤트 관리
12. 권한 관리
13. 신고 관리

## 기술 스택

### UI Components
- shadcn/ui (Button, Card, Table, Dialog, Select)
- Recharts (차트)
- React DnD (드래그 앤 드롭)
- React Image Lightbox (이미지 뷰어)

### 상태 관리
- Zustand (전역 상태)
- React Hook Form (폼 관리)
- Zod (유효성 검증)

### 데이터
- Supabase (백엔드)
- TanStack Table (테이블)
- Date-fns (날짜 처리)

### 유틸리티
- XLSX (엑셀 다운로드)
- Canvas Confetti (축하 효과)
- Framer Motion (애니메이션)
